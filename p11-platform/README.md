# P11 Platform

## Quick Start

1. Install dependencies: npm install
2. Add keys to .env
3. Run web: cd apps/web && npm run dev
4. Run python: cd services/data-engine && py -m uvicorn main:app --reload